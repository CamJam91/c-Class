for(int count = 0; count < target; count++)

for(int count = target; count > 0; count--)

variables: 
count is defined in our loop scope and is used to keep track of iterations
target is the known size of an item that we want to iterate through, it is used to stop the loop

incriment: starts at lower bound
decrement: starts at higher bound

